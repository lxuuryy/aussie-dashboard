import React from 'react'
import ChatDashboard from '@/app/(components)/ChatDashboard'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
        <ChatDashboard />
    </div>
  )
}

export default page