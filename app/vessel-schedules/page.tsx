import React from 'react'
import VesselDashboard from '../(components)/VesselDashboard'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
        <VesselDashboard />
    </div>
  )
}

export default page