import React from 'react'
import MelbournePort from '@/app/(components)/MelbournePort';

type Props = {}

const page = (props: Props) => {
  return (
    <div>
        <MelbournePort />
    </div>
  )
}

export default page